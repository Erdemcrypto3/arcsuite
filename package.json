{
  "name": "arcsuite",
  "version": "0.1.0",
  "private": true,
  "description": "Arc Suite — a portfolio of mini utilities for Circle's Arc L1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  }
}
